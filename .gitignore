# SQLite data — never commit the friends' weigh-ins
weighoff.db
weighoff.db-wal
weighoff.db-shm
*.db
*.sqlite
*.sqlite3

# Secrets / local env
.env
*.local

# systemd unit with a real passphrase (keep the template in deploy/ instead)
deploy/weigh-off.local.service

# Python noise
__pycache__/
*.pyc

# OS noise
.DS_Store
